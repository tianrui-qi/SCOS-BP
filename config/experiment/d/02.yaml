# @package _global_

defaults:
  - /experiment/d/base@_here_
  - _self_

name: d/02

data:
  split_type: MeasurementDependent
  channel_perm: true
  channel_drop: 1.0

objective:
  p_span_large: [0.5, 1.0]

trainer:
  max_epochs: 2640
  ckpt_load_path: ckpt/d/01/last.ckpt
  resume: true
