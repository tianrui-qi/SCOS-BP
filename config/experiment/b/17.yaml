# @package _global_

defaults:
  - /experiment/b/base@_here_
  - _self_

name: b/17

data:
  split_type: MeasurementDependent

objective:
  task:
    stepRegression: 1.0

trainer:
  ckpt_load_path: ckpt/b/16/last.ckpt
