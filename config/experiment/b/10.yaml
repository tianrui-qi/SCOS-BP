# @package _global_

defaults:
  - /experiment/b/base@_here_
  - _self_

name: b/10

data:
  split_type: MeasurementDependent
  batch_size: 1024

objective:
  freeze_embedding: true
  freeze_transformer: 4
  task:
    stepRegression: 1.0
  lr: 0.01

trainer:
  max_epochs: 3195
  ckpt_load_path: ckpt/b/06/epoch1004.ckpt
